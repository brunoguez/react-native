import * as React from 'react';
import { SafeAreaView } from 'react-native';
import { StyleSheet, Text, Safe } from 'react-native-paper';

const Login = () => {
  return (
    <SafeAreaView>
      <Text>Login</Text>
    </SafeAreaView>
  )
}

export default Login

const styles = StyleSheet.create({})